<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.iiitm.android.gwalior_tourism">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="Gwalior-Jharokha"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainsplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainEnglishActivity" />
        <activity android:name=".HistoricalActivity" />
        <activity android:name=".WorshipActivity" />
        <activity android:name=".NearbyActivity" />
        <activity android:name=".MedicalActivity" />
        <activity android:name=".KeyNumberActivity" />
        <activity android:name=".EnglishAboutActivity" />
        <activity android:name=".ToiletActivity" />
        <activity android:name=".HotelActivity" />
        <activity android:name=".LanguageScreen" />
        <activity android:name=".MainHindiActivity" />
        <activity android:name=".ToiletsHindiActivity" />
        <activity android:name=".KeyNumbersHindiActivity" />
        <activity android:name=".HindiAboutActivity" />
        <activity android:name=".cardslidertry.MainActivitySliderHistorical" />
        <activity android:name=".cardslidertry.MainActivitySliderWorship" />
        <activity android:name=".cardslidertry.MainActivitySliderNear" />
        <activity android:name=".cardslidertry.MainActivitySliderHotels" />
        <activity android:name=".cardslidertry.MainActivitySliderMedical" />
        <activity android:name=".cardslidertry.MainActivitySliderHindi" />

        <activity android:name=".MapsActivity2"/>
        <activity android:name=".MainTransportationActivity"/>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">

        </activity>
    </application>

</manifest>